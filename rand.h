#include "player.h"
//PURPOSE:: generates a completely random chess move
//ARGUMENTS:: the two X and Y values necesaary to store a move inside,
//            the chess board itself, and the two player variables.
void computer_move(int * Y1, int * Y2, int * X1, int * X2, 
    char B [8] [9], Player * P1);